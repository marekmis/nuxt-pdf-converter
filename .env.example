# Shared password required to access the app. Set this in Vercel → Settings → Environment Variables.
# The session cookie's signing key is derived from it, so changing it signs everyone out.
APP_PASSWORD=change-me
